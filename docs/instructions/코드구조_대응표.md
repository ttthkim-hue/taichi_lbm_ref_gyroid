# 코드 구조 대응표 (현재 vs §6 목표)

**기준:** `docs/[mainplan]_V1.1.md` §6  
**작성 일자:** 2026-03-17

---

## 1. 목표 디렉터리 구조 (§6)

```
project_root/
├── geometry/     # 형상 Taichi 커널 또는 .npy 마스크; STL 확인용·OpenSCAD 최종검증용
├── solver/       # taichi_lbm_core.py, post_process.py (ΔP, S_v, CV, NumPy, VTI)
├── optimization/ # bo_engine.py, run_experiment.py
└── results/      # vtk_plots/, pareto_data.csv, NumPy→OpenSCAD STL 최종검증
```

---

## 2. 현재 대응

| 목표 | 현재 위치/구현 |
|------|----------------|
| geometry (형상) | `geometry_openscad/` (3종 STL 확인용). .npy 마스크는 스크립트·아카이브에서 생성. |
| solver (LBM 코어) | 아카이브 `251224_coding/.../260107_Taichi_LBM/taichi_lbm_solver_v3.py` |
| solver (후처리) | 솔버 내 로깅·일부 후처리. 별도 `post_process.py` 통합 예정. |
| optimization | 파이프라인 정리됨, 실행은 사용자. |
| results | `Results_EmptyDuct_*`, `Results_Ref_dx02` 등. Pareto·NumPy→OpenSCAD 흐름 정리 예정. |

---

## 3. 검증

§6 "통합 시 위 디렉터리 구조로 정리 예정" 및 STL to Voxel 미채택, Taichi 커널→NumPy→OpenSCAD STL 흐름과 일치.

**검증:** §6 대조 완료.
